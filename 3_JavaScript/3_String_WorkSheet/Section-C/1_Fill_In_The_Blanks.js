/* 
1. Template literals are enclosed by "Backtick" instead of quotes. 
2. str.split("") will return an array of "Each Letter of str as an Element of array" . 
3. str.toLowerCase() is used to convert a string to "Lower Case String". 
4. str.replaceAll("a", "z") replaces "all" occurrences of "a". 
5. str.startsWith("web") returns a "boolean" value.
*/